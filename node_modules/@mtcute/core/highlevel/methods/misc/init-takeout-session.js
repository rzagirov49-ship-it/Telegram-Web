import { TakeoutSession as TakeoutSession$1 } from "../../types/misc/takeout-session.js";
async function initTakeoutSession(client, params) {
  return new TakeoutSession$1(
    client,
    await client.call({
      _: "account.initTakeoutSession",
      ...params
    })
  );
}
export {
  initTakeoutSession
};
