if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
function parseDateEntityFormat(format) {
  if (format === "r" || format === "R") {
    return { relative: true };
  }
  const result = {};
  for (const c of format) {
    switch (c) {
      case "t":
        result.shortTime = true;
        break;
      case "T":
        result.longTime = true;
        break;
      case "d":
        result.shortDate = true;
        break;
      case "D":
        result.longDate = true;
        break;
      case "w":
      case "W":
        result.dayOfWeek = true;
        break;
      default:
        throw new Error(`Invalid date format character: ${c}`);
    }
  }
  return result;
}
function dateEntityFormatToString(entity) {
  if (entity.relative) return "r";
  let result = "";
  if (entity.dayOfWeek) result += "w";
  if (entity.shortDate) result += "d";
  if (entity.longDate) result += "D";
  if (entity.shortTime) result += "t";
  if (entity.longTime) result += "T";
  return result;
}
exports.dateEntityFormatToString = dateEntityFormatToString;
exports.parseDateEntityFormat = parseDateEntityFormat;
