if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("../../types/utils.cjs");
const typeAssertions = require("../type-assertions.cjs");
const common = require("./common.cjs");
const botStart = /* @__PURE__ */ common.deeplinkBuilder({
  internalBuild: ({ username, parameter }) => ["resolve", { domain: username, start: parameter }],
  internalParse: (path, query) => {
    if (path !== "resolve") return null;
    const username = query.get("domain");
    const parameter = query.get("start");
    if (!username || !parameter) return null;
    return { username, parameter };
  },
  externalBuild: ({ username, parameter }) => [username, { start: parameter }],
  externalParse: (path, query) => {
    if (path.includes("/") || path[0] === "+" || path.length <= 2) return null;
    const username = path;
    const parameter = query.get("start");
    if (!parameter) return null;
    return { username, parameter };
  }
});
function normalizeBotAdmin(rights) {
  if (!rights?.length) return;
  return rights.map((it) => {
    switch (it) {
      case "changeInfo":
        return "change_info";
      case "postMessages":
        return "post_messages";
      case "editMessages":
        return "edit_messages";
      case "deleteMessages":
        return "delete_messages";
      case "banUsers":
        return "restrict_members";
      case "inviteUsers":
        return "invite_users";
      case "pinMessages":
        return "pin_messages";
      case "manageTopics":
        return "manage_topics";
      case "addAdmins":
        return "promote_members";
      case "manageCall":
        return "manage_video_chats";
      case "anonymous":
        return "anonymous";
      case "other":
        return "manage_chat";
      case "postStories":
        return "post_stories";
      case "editStories":
        return "edit_stories";
      case "deleteStories":
        return "delete_stories";
      case "manageDirectMessages":
        return "manage_chat";
      // idk?
      case "manageRanks":
        return "manage_chat";
      // idk?
      default:
        utils.assertNever();
        return "";
    }
  }).join("+");
}
function parseBotAdmin(rights) {
  if (!rights) return;
  return rights.split("+").map((it) => {
    switch (it) {
      case "change_info":
        return "changeInfo";
      case "post_messages":
        return "postMessages";
      case "edit_messages":
        return "editMessages";
      case "delete_messages":
        return "deleteMessages";
      case "restrict_members":
        return "banUsers";
      case "invite_users":
        return "inviteUsers";
      case "pin_messages":
        return "pinMessages";
      case "manage_topics":
        return "manageTopics";
      case "promote_members":
        return "addAdmins";
      case "manage_video_chats":
        return "manageCall";
      case "anonymous":
        return "anonymous";
      case "manage_chat":
        return "other";
      case "post_stories":
        return "postStories";
      case "edit_stories":
        return "editStories";
      case "delete_stories":
        return "deleteStories";
      default:
        return null;
    }
  }).filter(typeAssertions.isPresent);
}
const botAddToGroup = /* @__PURE__ */ common.deeplinkBuilder({
  internalBuild: ({ bot, parameter, admin }) => [
    "resolve",
    { domain: bot, startgroup: parameter ?? true, admin: normalizeBotAdmin(admin) }
  ],
  internalParse: (path, query) => {
    if (path !== "resolve") return null;
    const bot = query.get("domain");
    const parameter = query.get("startgroup");
    const admin = query.get("admin");
    if (!bot || parameter === null) return null;
    return {
      bot,
      parameter: parameter === "" ? void 0 : parameter,
      admin: parseBotAdmin(admin)
    };
  },
  externalBuild: ({ bot, parameter, admin }) => [
    bot,
    { startgroup: parameter ?? true, admin: normalizeBotAdmin(admin) }
  ],
  externalParse: (path, query) => {
    if (path.includes("/") || path[0] === "+" || path.length <= 2) return null;
    const bot = path;
    const parameter = query.get("startgroup");
    const admin = query.get("admin");
    if (parameter === null) return null;
    return {
      bot,
      parameter: parameter === "" ? void 0 : parameter,
      admin: parseBotAdmin(admin)
    };
  }
});
const botAddToChannel = /* @__PURE__ */ common.deeplinkBuilder({
  internalBuild: ({ bot, admin }) => [
    "resolve",
    { domain: bot, startchannel: true, admin: normalizeBotAdmin(admin) }
  ],
  internalParse: (path, query) => {
    if (path !== "resolve") return null;
    const bot = query.get("domain");
    const parameter = query.get("startchannel");
    const admin = query.get("admin");
    if (!bot || parameter === null) return null;
    return {
      bot,
      admin: parseBotAdmin(admin)
    };
  },
  externalBuild: ({ bot, admin }) => [bot, { startchannel: true, admin: normalizeBotAdmin(admin) }],
  externalParse: (path, query) => {
    if (path.includes("/") || path[0] === "+" || path.length <= 2) return null;
    const bot = path;
    const parameter = query.get("startchannel");
    const admin = query.get("admin");
    if (parameter === null) return null;
    return {
      bot,
      admin: parseBotAdmin(admin)
    };
  }
});
const botGame = /* @__PURE__ */ common.deeplinkBuilder({
  internalBuild: ({ bot, game }) => ["resolve", { domain: bot, game }],
  internalParse: (path, query) => {
    if (path !== "resolve") return null;
    const bot = query.get("domain");
    const game = query.get("game");
    if (!bot || !game) return null;
    return { bot, game };
  },
  externalBuild: ({ bot, game }) => [bot, { game }],
  externalParse: (path, query) => {
    if (path.includes("/") || path[0] === "+" || path.length <= 2) return null;
    const bot = path;
    const game = query.get("game");
    if (!game) return null;
    return { bot, game };
  }
});
const botWebApp = /* @__PURE__ */ common.deeplinkBuilder({
  internalBuild: ({ bot, app, parameter }) => ["resolve", { domain: bot, appname: app, startapp: parameter }],
  internalParse: (path, query) => {
    if (path !== "resolve") return null;
    const bot = query.get("domain");
    const app = query.get("appname");
    const parameter = query.get("startapp");
    if (!bot || !app) return null;
    return { bot, app, parameter: parameter || void 0 };
  },
  externalBuild: ({ bot, app, parameter }) => [`${bot}/${app}`, { startapp: parameter }],
  externalParse: (path, query) => {
    const [bot, app, rest] = path.split("/");
    if (!app || rest) return null;
    const parameter = query.get("startapp");
    return { bot, app, parameter: parameter || void 0 };
  }
});
exports.botAddToChannel = botAddToChannel;
exports.botAddToGroup = botAddToGroup;
exports.botGame = botGame;
exports.botStart = botStart;
exports.botWebApp = botWebApp;
