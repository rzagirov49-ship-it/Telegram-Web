if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const utils = require("../../../types/utils.cjs");
const typeAssertions = require("../../../utils/type-assertions.cjs");
const documentUtils = require("../../types/media/document-utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const normalizeInputMedia = require("./normalize-input-media.cjs");
const photo = require("../../types/media/photo.cjs");
async function uploadMedia(client, media, params = {}) {
  const normMedia = await normalizeInputMedia._normalizeInputMedia(client, media, params, false);
  switch (normMedia._) {
    case "inputMediaEmpty":
    case "inputMediaGeoPoint":
    case "inputMediaGeoLive":
    case "inputMediaContact":
    case "inputMediaVenue":
    case "inputMediaGame":
    case "inputMediaInvoice":
    case "inputMediaPoll":
    case "inputMediaDice":
    case "inputMediaPaidMedia":
    case "inputMediaTodo":
    case "inputMediaStakeDice":
      throw new errors.MtArgumentError(`This media (${normMedia._}) can't be uploaded`);
  }
  const res = await client.call({
    _: "messages.uploadMedia",
    peer: params.peer ? await resolvePeer.resolvePeer(client, params.peer) : {
      _: "inputPeerSelf"
    },
    media: normMedia
  });
  typeAssertions.assertTypeIsNot("uploadMedia", res, "messageMediaEmpty");
  switch (normMedia._) {
    case "inputMediaUploadedPhoto":
    case "inputMediaPhoto":
    case "inputMediaPhotoExternal":
      typeAssertions.assertTypeIs("uploadMedia", res, "messageMediaPhoto");
      typeAssertions.assertTypeIs("uploadMedia", res.photo, "photo");
      return new photo.Photo(res.photo);
    case "inputMediaUploadedDocument":
    case "inputMediaDocument":
    case "inputMediaDocumentExternal":
      typeAssertions.assertTypeIs("uploadMedia", res, "messageMediaDocument");
      typeAssertions.assertTypeIs("uploadMedia", res.document, "document");
      return documentUtils.parseDocument(res.document, res);
    case "inputMediaStory":
    case "inputMediaWebPage":
      throw new errors.MtArgumentError(`This media (${normMedia._}) can't be uploaded`);
    default:
      utils.assertNever();
  }
}
exports.uploadMedia = uploadMedia;
