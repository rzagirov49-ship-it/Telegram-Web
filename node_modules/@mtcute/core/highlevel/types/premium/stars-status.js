import { makeInspectable } from "../../utils/inspectable.js";
import { memoizeGetters } from "../../utils/memoize.js";
import { PeersIndex } from "../peers/peers-index.js";
import { StarsTransaction as StarsTransaction$2 } from "./stars-transaction.js";
class StarsStatus {
  constructor(raw) {
    this.raw = raw;
    this.peers = PeersIndex.from(raw);
  }
  peers;
  /** Current Telegram Stars balance */
  get balance() {
    return this.raw.balance;
  }
  /**
   * History of Telegram Stars transactions
   */
  get transactions() {
    return this.raw.history?.map((it) => new StarsTransaction$2(it, this.peers)) ?? [];
  }
  /** Next offset of {@link transactions} for pagination */
  get transactionsNextOffset() {
    return this.raw.nextOffset ?? null;
  }
}
const StarsStatus$1 = /* @__PURE__ */ makeInspectable(StarsStatus);
const StarsStatus$2 = /* @__PURE__ */ memoizeGetters(StarsStatus$1, ["transactions"]);
export {
  StarsStatus$2 as StarsStatus
};
