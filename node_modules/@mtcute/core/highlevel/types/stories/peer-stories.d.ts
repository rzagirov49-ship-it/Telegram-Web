import { tl } from '../../../tl/index.js';
import { Peer, PeersIndex } from '../peers/index.js';
import { Story } from './story.js';
export declare class PeerStories {
    readonly raw: tl.RawPeerStories;
    readonly _peers: PeersIndex;
    constructor(raw: tl.RawPeerStories, _peers: PeersIndex);
    /**
     * Peer that owns these stories.
     */
    get peer(): Peer;
    /**
     * ID of the last read story of this peer.
     */
    get maxReadId(): number;
    /**
     * List of peer stories.
     */
    get stories(): Story[];
}
