if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const getProfilePhotos = require("./get-profile-photos.cjs");
const resolvePeer = require("./resolve-peer.cjs");
async function* iterProfilePhotos(client, userId, params) {
  if (!params) params = {};
  const peer = await resolvePeer.resolveUser(client, userId);
  const { limit = Infinity, chunkSize = 100 } = params;
  let { offset } = params;
  let current = 0;
  for (; ; ) {
    const res = await getProfilePhotos.getProfilePhotos(client, peer, {
      offset,
      limit: Math.min(chunkSize, limit - current)
    });
    for (const it of res) {
      yield it;
      if (++current >= limit) return;
    }
    if (!res.next) return;
    offset = res.next;
  }
}
exports.iterProfilePhotos = iterProfilePhotos;
