import { BaseTelegramClient } from '../base.js';
import { AppConfigSchema } from '../types/misc/app-config.js';
export declare class AppConfigManager {
    private client;
    private _resource;
    constructor(client: BaseTelegramClient);
    private _object?;
    get(): Promise<AppConfigSchema>;
    getField<K extends keyof AppConfigSchema>(field: K): Promise<AppConfigSchema[K]>;
    getField<K extends keyof AppConfigSchema>(field: K, fallback: NonNullable<AppConfigSchema[K]>): Promise<NonNullable<AppConfigSchema[K]>>;
}
