"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
function splitOnce(str, separator) {
  const idx = str.indexOf(separator);
  if (idx === -1) {
    throw new Error(`Separator not found: ${separator}`, {
      cause: { str, separator }
    });
  }
  return [str.slice(0, idx), str.slice(idx + separator.length)];
}
function assertStartsWith(str, prefix) {
  if (!str.startsWith(prefix)) {
    throw new Error(`String does not start with ${prefix}`, {
      cause: { str, prefix }
    });
  }
}
function assertEndsWith(str, suffix) {
  if (!str.endsWith(suffix)) {
    throw new Error(`String does not end with ${suffix}`, {
      cause: { str, suffix }
    });
  }
}
exports.assertEndsWith = assertEndsWith;
exports.assertStartsWith = assertStartsWith;
exports.splitOnce = splitOnce;
