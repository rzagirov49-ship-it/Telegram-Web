"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const empty = new Uint8Array(0);
function clone(buf) {
  return new Uint8Array(buf);
}
function readNthBit(byte, bit) {
  return (byte & 1 << bit) >> bit;
}
exports.clone = clone;
exports.empty = empty;
exports.readNthBit = readNthBit;
