if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("@fuman/utils");
function generateKeyAndIvFromNonce(crypto, serverNonce, newNonce) {
  const hash1 = crypto.sha1(utils.u8.concat2(newNonce, serverNonce));
  const hash2 = crypto.sha1(utils.u8.concat2(serverNonce, newNonce));
  const hash3 = crypto.sha1(utils.u8.concat2(newNonce, newNonce));
  const key = utils.u8.concat2(hash1, hash2.subarray(0, 12));
  const iv = utils.u8.concat3(hash2.subarray(12, 20), hash3, newNonce.subarray(0, 4));
  return [key, iv];
}
function createAesIgeForMessage(crypto, authKey, messageKey, client) {
  const x = client ? 0 : 8;
  const sha256a = crypto.sha256(utils.u8.concat2(messageKey, authKey.subarray(x, 36 + x)));
  const sha256b = crypto.sha256(utils.u8.concat2(authKey.subarray(40 + x, 76 + x), messageKey));
  const key = utils.u8.concat3(sha256a.subarray(0, 8), sha256b.subarray(8, 24), sha256a.subarray(24, 32));
  const iv = utils.u8.concat3(sha256b.subarray(0, 8), sha256a.subarray(8, 24), sha256b.subarray(24, 32));
  return crypto.createAesIge(key, iv);
}
function createAesIgeForMessageOld(crypto, authKey, messageKey, client) {
  const x = client ? 0 : 8;
  const sha1a = crypto.sha1(utils.u8.concat2(messageKey, authKey.subarray(x, 32 + x)));
  const sha1b = crypto.sha1(
    utils.u8.concat3(authKey.subarray(32 + x, 48 + x), messageKey, authKey.subarray(48 + x, 64 + x))
  );
  const sha1c = crypto.sha1(utils.u8.concat2(authKey.subarray(64 + x, 96 + x), messageKey));
  const sha1d = crypto.sha1(utils.u8.concat2(messageKey, authKey.subarray(96 + x, 128 + x)));
  const key = utils.u8.concat3(sha1a.subarray(0, 8), sha1b.subarray(8, 20), sha1c.subarray(4, 16));
  const iv = utils.u8.concat([
    sha1a.subarray(8, 20),
    sha1b.subarray(0, 8),
    sha1c.subarray(16, 20),
    sha1d.subarray(0, 8)
  ]);
  return crypto.createAesIge(key, iv);
}
exports.createAesIgeForMessage = createAesIgeForMessage;
exports.createAesIgeForMessageOld = createAesIgeForMessageOld;
exports.generateKeyAndIvFromNonce = generateKeyAndIvFromNonce;
