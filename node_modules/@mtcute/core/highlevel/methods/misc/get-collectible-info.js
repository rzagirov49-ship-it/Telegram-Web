import { CollectibleInfo as CollectibleInfo$1 } from "../../types/misc/collectible-info.js";
import { normalizePhoneNumber } from "../../utils/misc-utils.js";
async function getCollectibleInfo(client, kind, item) {
  const res = await client.call({
    _: "fragment.getCollectibleInfo",
    collectible: kind === "phone" ? {
      _: "inputCollectiblePhone",
      phone: normalizePhoneNumber(item)
    } : {
      _: "inputCollectibleUsername",
      username: item
    }
  });
  return new CollectibleInfo$1(res);
}
export {
  getCollectibleInfo
};
