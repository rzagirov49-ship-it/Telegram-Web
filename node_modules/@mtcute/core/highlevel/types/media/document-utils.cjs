if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const audio = require("./audio.cjs");
const document = require("./document.cjs");
const sticker = require("./sticker.cjs");
const video = require("./video.cjs");
const voice = require("./voice.cjs");
function parseSticker(doc) {
  const stickerAttr = doc.attributes.find(
    (a) => a._ === "documentAttributeSticker" || a._ === "documentAttributeCustomEmoji"
  );
  if (stickerAttr) {
    const sz = doc.attributes.find(
      (it) => it._ === "documentAttributeImageSize" || it._ === "documentAttributeVideo"
    );
    return new sticker.Sticker(doc, stickerAttr, sz);
  }
}
function parseDocument(doc, media) {
  const sticker2 = parseSticker(doc);
  if (sticker2) return sticker2;
  for (const attr of doc.attributes) {
    switch (attr._) {
      case "documentAttributeAudio":
        if (attr.voice) {
          return new voice.Voice(doc, attr, media);
        }
        return new audio.Audio(doc, attr);
      case "documentAttributeVideo":
        return new video.Video(doc, attr, media);
      case "documentAttributeImageSize":
        if (doc.mimeType === "image/gif") {
          return new video.Video(doc, attr, media);
        }
    }
  }
  return new document.Document(doc);
}
exports.parseDocument = parseDocument;
exports.parseSticker = parseSticker;
