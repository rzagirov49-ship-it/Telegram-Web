if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("@fuman/utils");
const longUtils = require("../../../utils/long-utils.cjs");
const documentUtils = require("../../types/media/document-utils.cjs");
async function getCustomEmojis(client, ids) {
  const res = await client.call({
    _: "messages.getCustomEmojiDocuments",
    documentId: ids
  });
  return res.map((it) => {
    if (it._ !== "document") return null;
    const doc = documentUtils.parseDocument(it);
    if (doc.type !== "sticker") {
      return null;
    }
    return doc;
  });
}
async function getCustomEmojisFromMessages(client, messages) {
  const set = new longUtils.LongSet();
  if (!Array.isArray(messages)) messages = [messages];
  for (const { raw } of messages) {
    if (raw._ === "messageService" || !raw.entities) continue;
    for (const entity of raw.entities) {
      if (entity._ === "messageEntityCustomEmoji") {
        set.add(entity.documentId);
      }
    }
  }
  const arr = [...set];
  if (!arr.length) return [];
  return getCustomEmojis(client, arr).then((res) => res.filter(utils.isNotNull));
}
exports.getCustomEmojis = getCustomEmojis;
exports.getCustomEmojisFromMessages = getCustomEmojisFromMessages;
