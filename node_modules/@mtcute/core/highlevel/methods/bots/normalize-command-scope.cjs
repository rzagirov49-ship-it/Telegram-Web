if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("../../../types/utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const innerTl = require("../../../tl/inner-tl.cjs");
async function _normalizeCommandScope(client, scope) {
  if (innerTl.isAnyBotCommandScope(scope)) return scope;
  switch (scope.type) {
    case "peer":
    case "peer_admins": {
      const peer = await resolvePeer.resolvePeer(client, scope.peer);
      return {
        _: scope.type === "peer" ? "botCommandScopePeer" : "botCommandScopePeerAdmins",
        peer
      };
    }
    case "member": {
      const user = await resolvePeer.resolveUser(client, scope.user);
      const chat = await resolvePeer.resolvePeer(client, scope.chat);
      return {
        _: "botCommandScopePeerUser",
        peer: chat,
        userId: user
      };
    }
    default:
      utils.assertNever();
  }
}
exports._normalizeCommandScope = _normalizeCommandScope;
