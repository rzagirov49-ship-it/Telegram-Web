import { makeInspectable } from "../../utils/inspectable.js";
class StoriesStealthMode {
  constructor(raw) {
    this.raw = raw;
  }
  /** Stealth mode is active until this date */
  get activeUntil() {
    if (!this.raw.activeUntilDate) return null;
    return new Date(this.raw.activeUntilDate * 1e3);
  }
  /** Stealth mode is having a cooldown until this date */
  get cooldownUntil() {
    if (!this.raw.cooldownUntilDate) return null;
    return new Date(this.raw.cooldownUntilDate * 1e3);
  }
}
const StoriesStealthMode$1 = /* @__PURE__ */ makeInspectable(StoriesStealthMode);
export {
  StoriesStealthMode$1 as StoriesStealthMode
};
