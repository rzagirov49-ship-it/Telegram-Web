function toDataView(buf) {
  return new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
}
function view(ctor, buf) {
  return new ctor(buf.buffer, buf.byteOffset, buf.byteLength / ctor.BYTES_PER_ELEMENT);
}
let _cachedByteOrder = null;
function getPlatformByteOrder() {
  if (_cachedByteOrder === null) {
    const buffer = new ArrayBuffer(2);
    const view2 = new DataView(buffer);
    view2.setUint16(0, 4660, true);
    _cachedByteOrder = new Uint16Array(buffer)[0] === 4660;
  }
  return _cachedByteOrder ? "little" : "big";
}
export {
  getPlatformByteOrder,
  toDataView,
  view
};
