import { TlBinaryWriter, TlBinaryReader } from "@mtcute/tl-runtime";
class BaseService {
  _driver;
  _readerMap;
  _writerMap;
  _log;
  constructor(opts) {
    this._driver = opts.driver;
    this._readerMap = opts.readerMap;
    this._writerMap = opts.writerMap;
    this._log = opts.log;
  }
  _serializeTl(obj) {
    return TlBinaryWriter.serializeObject(this._writerMap, obj);
  }
  _deserializeTl(data) {
    try {
      return TlBinaryReader.deserializeObject(this._readerMap, data);
    } catch {
      return null;
    }
  }
}
export {
  BaseService
};
