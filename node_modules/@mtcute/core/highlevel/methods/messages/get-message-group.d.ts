import { ITelegramClient } from '../../client.types.js';
import { InputMessageId, Message } from '../../types/index.js';
/**
 * Get all messages inside of a message group
 *
 * @param chatId  Chat ID
 * @param message  ID of one of the messages in the group
 */
export declare function getMessageGroup(client: ITelegramClient, params: InputMessageId): Promise<Message[]>;
