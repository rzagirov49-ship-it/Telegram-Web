if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("@fuman/utils");
const bigintUtils = require("../bigint-utils.cjs");
function millerRabin(crypto, n, rounds = 20) {
  if (n < 4n) return n > 1n;
  if (n % 2n === 0n || n < 0n) return false;
  const nBits = utils.bigint.bitLength(n);
  const nSub = n - 1n;
  const r = utils.bigint.twoMultiplicity(nSub);
  const d = nSub >> r;
  for (let i = 0; i < rounds; i++) {
    let base;
    do {
      base = bigintUtils.randomBigIntBits(crypto, nBits);
    } while (base <= 1n || base >= nSub);
    let x = utils.bigint.modPowBinary(base, d, n);
    if (x === 1n || x === nSub) continue;
    let i2 = 0n;
    let y;
    while (i2 < r) {
      y = utils.bigint.modPowBinary(x, 2n, n);
      if (x === 1n) return false;
      if (x === nSub) break;
      i2 += 1n;
      x = y;
    }
    if (i2 === r) return false;
  }
  return true;
}
exports.millerRabin = millerRabin;
