if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const peerUtils = require("../../../utils/peer-utils.cjs");
const typeAssertions = require("../../../utils/type-assertions.cjs");
const utils = require("../../updates/utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const inputMessageId = require("../../types/messages/input-message-id.cjs");
const innerTl = require("../../../tl/inner-tl.cjs");
const peersIndex = require("../../types/peers/peers-index.cjs");
const messageReactions = require("../../types/messages/message-reactions.cjs");
async function sendPaidReaction(client, params) {
  const { anonymous, asPeer, count = 1 } = params;
  const { chatId, message } = inputMessageId.normalizeInputMessageId(params);
  const peer = await resolvePeer.resolvePeer(client, chatId);
  let privacy;
  if (anonymous !== void 0) {
    privacy = { _: anonymous ? "paidReactionPrivacyAnonymous" : "paidReactionPrivacyDefault" };
  } else if (asPeer) {
    privacy = { _: "paidReactionPrivacyPeer", peer: await resolvePeer.resolvePeer(client, asPeer) };
  }
  let res;
  for (let i = 0; i < 3; i++) {
    try {
      res = await client.call({
        _: "messages.sendPaidReaction",
        peer,
        msgId: message,
        count,
        private: privacy,
        randomId: await client.getMtprotoMessageId()
      });
      break;
    } catch (e) {
      if (innerTl.RpcError.is(e, "RANDOM_ID_EXPIRED")) {
        continue;
      }
      throw e;
    }
  }
  if (!res) {
    throw new errors.MtcuteError("Could not send paid reaction");
  }
  utils.assertIsUpdatesGroup("messages.sendReaction", res);
  const peers = peersIndex.PeersIndex.from(res);
  const upd = res.updates[0];
  typeAssertions.assertTypeIs("messages.sendPaidReaction (@ .updates[0])", upd, "updateMessageReactions");
  return new messageReactions.MessageReactions(upd.msgId, peerUtils.getMarkedPeerId(upd.peer), upd.reactions, peers);
}
exports.sendPaidReaction = sendPaidReaction;
