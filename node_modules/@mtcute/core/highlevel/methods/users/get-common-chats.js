import { resolveUser } from "./resolve-peer.js";
import { Chat as Chat$2 } from "../../types/peers/chat.js";
async function getCommonChats(client, userId) {
  return client.call({
    _: "messages.getCommonChats",
    userId: await resolveUser(client, userId),
    maxId: 0,
    limit: 100
  }).then((res) => res.chats.map((it) => new Chat$2(it)));
}
export {
  getCommonChats
};
