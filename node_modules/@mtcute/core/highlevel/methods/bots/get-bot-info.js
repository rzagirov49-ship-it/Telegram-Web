import { resolveUser } from "../users/resolve-peer.js";
async function getBotInfo(client, params) {
  const { bot, langCode = "" } = params;
  return client.call({
    _: "bots.getBotInfo",
    bot: bot ? await resolveUser(client, bot) : void 0,
    langCode
  });
}
export {
  getBotInfo
};
