import { tl } from '../../../tl/index.js';
/**
 * Input version of {@link ReactionEmoji}, which also accepts bare TL object
 */
export type InputReaction = string | tl.Long | tl.TypeReaction;
/**
 * Emoji describing a reaction.
 *
 * Either a `string` with a unicode emoji, or a `tl.Long` for a custom emoji
 *
 * For paid reactions, will always contain `⭐` emoji
 */
export type ReactionEmoji = string | tl.Long;
export declare function normalizeInputReaction(reaction?: InputReaction | null): tl.TypeReaction;
export declare function toReactionEmoji(reaction: tl.TypeReaction, allowEmpty?: false): ReactionEmoji;
export declare function toReactionEmoji(reaction: tl.TypeReaction, allowEmpty: true): ReactionEmoji | null;
