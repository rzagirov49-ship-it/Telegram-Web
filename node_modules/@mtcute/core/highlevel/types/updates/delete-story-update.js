import { memoizeGetters } from "../../utils/memoize.js";
import { makeInspectable } from "../../utils/inspectable.js";
import { parsePeer } from "../peers/peer.js";
class DeleteStoryUpdate {
  constructor(raw, _peers) {
    this.raw = raw;
    this._peers = _peers;
  }
  /**
   * Peer that owns these stories.
   */
  get peer() {
    return parsePeer(this.raw.peer, this._peers);
  }
  /**
   * ID of the deleted story
   */
  get storyId() {
    return this.raw.story.id;
  }
}
const DeleteStoryUpdate$1 = /* @__PURE__ */ memoizeGetters(DeleteStoryUpdate, ["peer"]);
const DeleteStoryUpdate$2 = /* @__PURE__ */ makeInspectable(DeleteStoryUpdate$1);
export {
  DeleteStoryUpdate$2 as DeleteStoryUpdate
};
