if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const typeAssertions = require("../../../utils/type-assertions.cjs");
const businessWorkHours = require("../../types/premium/business-work-hours.cjs");
async function setBusinessWorkHours(client, params) {
  let businessWorkHours$1;
  if (params) {
    let weeklyOpen;
    if ("hours" in params) {
      weeklyOpen = businessWorkHours.businessWorkHoursDaysToRaw(params.hours);
    } else {
      weeklyOpen = params.intervals;
    }
    businessWorkHours$1 = {
      _: "businessWorkHours",
      timezoneId: params.timezone,
      weeklyOpen
    };
  }
  const res = await client.call({
    _: "account.updateBusinessWorkHours",
    businessWorkHours: businessWorkHours$1
  });
  typeAssertions.assertTrue("account.updateBusinessWorkHours", res);
}
exports.setBusinessWorkHours = setBusinessWorkHours;
