import { assertTypeIs } from "../../../utils/type-assertions.js";
import { memoizeGetters } from "../../utils/memoize.js";
import { Story as Story$2 } from "../stories/story.js";
import { makeInspectable } from "../../utils/inspectable.js";
import { parsePeer } from "../peers/peer.js";
class StoryUpdate {
  constructor(raw, _peers) {
    this.raw = raw;
    this._peers = _peers;
  }
  /**
   * Peer that owns these stories.
   */
  get peer() {
    return parsePeer(this.raw.peer, this._peers);
  }
  /**
   * Story that was posted or edited.
   */
  get story() {
    assertTypeIs("StoryUpdate.story", this.raw.story, "storyItem");
    return new Story$2(this.raw.story, this._peers);
  }
}
const StoryUpdate$1 = /* @__PURE__ */ memoizeGetters(StoryUpdate, ["peer", "story"]);
const StoryUpdate$2 = /* @__PURE__ */ makeInspectable(StoryUpdate$1);
export {
  StoryUpdate$2 as StoryUpdate
};
