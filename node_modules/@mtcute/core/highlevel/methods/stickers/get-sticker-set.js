import { normalizeInputStickerSet, StickerSet as StickerSet$2 } from "../../types/misc/sticker-set.js";
async function getStickerSet(client, setId) {
  const res = await client.call({
    _: "messages.getStickerSet",
    stickerset: normalizeInputStickerSet(setId),
    hash: 0
  });
  return new StickerSet$2(res);
}
export {
  getStickerSet
};
