if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const resolvePeer = require("../users/resolve-peer.cjs");
const chatInviteLinkMember = require("../../types/peers/chat-invite-link-member.cjs");
const miscUtils = require("../../utils/misc-utils.cjs");
const peersIndex = require("../../types/peers/peers-index.cjs");
const peerUtils = require("../../utils/peer-utils.cjs");
async function getInviteLinkMembers(client, chatId, params) {
  const peer = await resolvePeer.resolvePeer(client, chatId);
  if (!params) params = {};
  const { limit = 100, link, requestedSearch, requested = Boolean(requestedSearch) } = params;
  const { offsetUser = { _: "inputUserEmpty" } } = params;
  const offsetDate = miscUtils.normalizeDate(params.offsetDate) ?? 0;
  const res = await client.call({
    _: "messages.getChatInviteImporters",
    limit,
    peer,
    link: typeof link === "string" ? link : link?.link,
    requested,
    q: requestedSearch,
    offsetDate,
    offsetUser
  });
  const peers = peersIndex.PeersIndex.from(res);
  const members = res.importers.map((it) => new chatInviteLinkMember.ChatInviteLinkMember(it, peers));
  const last = members[members.length - 1];
  const nextOffset = last ? {
    date: last.raw.date,
    user: peerUtils.toInputUser(last.user.inputPeer)
  } : void 0;
  return miscUtils.makeArrayPaginated(members, res.count, nextOffset);
}
exports.getInviteLinkMembers = getInviteLinkMembers;
