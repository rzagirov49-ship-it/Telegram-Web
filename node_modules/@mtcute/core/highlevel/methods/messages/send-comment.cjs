if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const sendMediaGroup = require("./send-media-group.cjs");
const sendMedia = require("./send-media.cjs");
const sendReply = require("./send-reply.cjs");
const sendText = require("./send-text.cjs");
function commentText(client, message, ...params) {
  if (message.chat.type !== "chat" || message.chat.chatType !== "channel") {
    return sendReply.replyText(client, message, ...params);
  }
  if (!message.replies || !message.replies.hasComments) {
    throw new errors.MtArgumentError("This message does not have comments section");
  }
  const [text, params_ = {}] = params;
  params_.commentTo = message.id;
  return sendText.sendText(client, message.chat.inputPeer, text, params_);
}
function commentMedia(client, message, ...params) {
  if (message.chat.type !== "chat" || message.chat.chatType !== "channel") {
    return sendReply.replyMedia(client, message, ...params);
  }
  if (!message.replies || !message.replies.hasComments) {
    throw new errors.MtArgumentError("This message does not have comments section");
  }
  const [media, params_ = {}] = params;
  params_.commentTo = message.id;
  return sendMedia.sendMedia(client, message.chat.inputPeer, media, params_);
}
function commentMediaGroup(client, message, ...params) {
  if (message.chat.type !== "chat" || message.chat.chatType !== "channel") {
    return sendReply.replyMediaGroup(client, message, ...params);
  }
  if (!message.replies || !message.replies.hasComments) {
    throw new errors.MtArgumentError("This message does not have comments section");
  }
  const [media, params_ = {}] = params;
  params_.commentTo = message.id;
  return sendMediaGroup.sendMediaGroup(client, message.chat.inputPeer, media, params_);
}
exports.commentMedia = commentMedia;
exports.commentMediaGroup = commentMediaGroup;
exports.commentText = commentText;
