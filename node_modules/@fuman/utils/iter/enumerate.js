function enumerate(iterable) {
  const iterator = iterable[Symbol.iterator]();
  let idx = 0;
  return {
    [Symbol.iterator]() {
      return this;
    },
    next() {
      const res = iterator.next();
      if (res.done) {
        return res;
      }
      return {
        done: false,
        value: [idx++, res.value]
      };
    }
  };
}
export {
  enumerate
};
