import { StoryInteractiveArea } from "./base.js";
import { toReactionEmoji } from "../../reactions/types.js";
import { makeInspectable } from "../../../utils/inspectable.js";
class StoryInteractiveReaction extends StoryInteractiveArea {
  constructor(raw) {
    super(raw);
    this.raw = raw;
  }
  type = "reaction";
  /** Whether this reaction is on a dark background */
  get isDark() {
    return this.raw.dark;
  }
  /** Whether this reaction is flipped (i.e. has tail on the left) */
  get isFlipped() {
    return this.raw.flipped;
  }
  /** Emoji representing the reaction */
  get emoji() {
    return toReactionEmoji(this.raw.reaction);
  }
}
const StoryInteractiveReaction$1 = /* @__PURE__ */ makeInspectable(StoryInteractiveReaction);
export {
  StoryInteractiveReaction$1 as StoryInteractiveReaction
};
