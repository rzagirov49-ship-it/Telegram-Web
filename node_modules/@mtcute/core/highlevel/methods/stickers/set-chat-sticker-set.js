import { assertTrue } from "../../../utils/type-assertions.js";
import { resolveChannel } from "../users/resolve-peer.js";
import { normalizeInputStickerSet } from "../../types/misc/sticker-set.js";
async function setChatStickerSet(client, chatId, setId) {
  const r = await client.call({
    _: "channels.setStickers",
    channel: await resolveChannel(client, chatId),
    stickerset: normalizeInputStickerSet(setId)
  });
  assertTrue("channels.setStickers", r);
}
export {
  setChatStickerSet
};
