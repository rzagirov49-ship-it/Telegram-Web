if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const typeAssertions = require("../../../utils/type-assertions.cjs");
const businessChatLink = require("../../types/premium/business-chat-link.cjs");
const normalizeText = require("../misc/normalize-text.cjs");
async function editBusinessChatLink(client, link, params) {
  const [message, entities] = await normalizeText._normalizeInputText(client, params.text);
  const res = await client.call({
    _: "account.editBusinessChatLink",
    slug: link instanceof businessChatLink.BusinessChatLink ? link.link : link,
    link: {
      _: "inputBusinessChatLink",
      message,
      entities,
      title: params?.title
    }
  });
  return new businessChatLink.BusinessChatLink(res);
}
async function deleteBusinessChatLink(client, link) {
  const res = await client.call({
    _: "account.deleteBusinessChatLink",
    slug: typeof link === "string" ? link : link.link
  });
  typeAssertions.assertTrue("account.deleteBusinessChatLink", res);
}
exports.deleteBusinessChatLink = deleteBusinessChatLink;
exports.editBusinessChatLink = editBusinessChatLink;
