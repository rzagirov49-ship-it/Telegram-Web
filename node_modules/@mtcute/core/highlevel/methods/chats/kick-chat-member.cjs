if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const miscUtils = require("../../../utils/misc-utils.cjs");
const peerUtils = require("../../utils/peer-utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const banChatMember = require("./ban-chat-member.cjs");
const unbanChatMember = require("./unban-chat-member.cjs");
async function kickChatMember(client, params) {
  const { chatId, userId } = params;
  const chat = await resolvePeer.resolvePeer(client, chatId);
  const user = await resolvePeer.resolvePeer(client, userId);
  const msg = await banChatMember.banChatMember(client, { chatId: chat, participantId: user });
  if (peerUtils.isInputPeerChannel(chat)) {
    await miscUtils.sleepWithAbort(1e3, client.stopSignal);
    await unbanChatMember.unbanChatMember(client, { chatId: chat, participantId: user });
  }
  return msg;
}
exports.kickChatMember = kickChatMember;
