if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors$1 = require("../../../types/errors.cjs");
const peerUtils = require("../../../utils/peer-utils.cjs");
const typeAssertions = require("../../../utils/type-assertions.cjs");
const utils = require("../../updates/utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const getMessages = require("./get-messages.cjs");
const inputMessageId = require("../../types/messages/input-message-id.cjs");
const errors = require("../../types/errors.cjs");
const poll = require("../../types/media/poll.cjs");
const peersIndex = require("../../types/peers/peers-index.cjs");
async function sendVote(client, params) {
  const { chatId, message } = inputMessageId.normalizeInputMessageId(params);
  let { options } = params;
  if (options === null) options = [];
  if (!Array.isArray(options)) options = [options];
  const peer = await resolvePeer.resolvePeer(client, chatId);
  let poll$1;
  if (options.some((it) => typeof it === "number")) {
    const [msg] = await getMessages.getMessages(client, peer, message);
    if (!msg) {
      throw new errors.MtMessageNotFoundError(peerUtils.getMarkedPeerId(peer), message, "to vote in");
    }
    if (!(msg.media instanceof poll.Poll)) {
      throw new errors$1.MtArgumentError("This message does not contain a poll");
    }
    poll$1 = msg.media;
    options = options.map((opt) => {
      if (typeof opt === "number") {
        const answer = poll$1.raw.answers[opt];
        typeAssertions.assertTypeIsNot("Poll.answers", answer, "inputPollAnswer");
        return answer.option;
      }
      return opt;
    });
  }
  const res = await client.call({
    _: "messages.sendVote",
    peer,
    msgId: message,
    options
  });
  utils.assertIsUpdatesGroup("messages.sendVote", res);
  client.handleClientUpdate(res, true);
  const upd = res.updates[0];
  typeAssertions.assertTypeIs("messages.sendVote (@ .updates[0])", upd, "updateMessagePoll");
  if (!upd.poll) {
    throw new errors$1.MtTypeAssertionError("messages.sendVote (@ .updates[0].poll)", "poll", "undefined");
  }
  const peers = peersIndex.PeersIndex.from(res);
  return new poll.Poll(upd.poll, peers, upd.results);
}
exports.sendVote = sendVote;
