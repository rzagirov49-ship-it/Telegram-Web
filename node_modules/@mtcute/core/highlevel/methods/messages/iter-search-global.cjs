if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const searchGlobal = require("./search-global.cjs");
const searchFilters = require("../../types/messages/search-filters.cjs");
const miscUtils = require("../../utils/misc-utils.cjs");
async function* iterSearchGlobal(client, params) {
  if (!params) params = {};
  const { query = "", filter = searchFilters.SearchFilters.Empty, limit = Infinity, chunkSize = 100, onlyChannels } = params;
  const minDate = miscUtils.normalizeDate(params.minDate) ?? 0;
  const maxDate = miscUtils.normalizeDate(params.maxDate) ?? 0;
  let { offset } = params;
  let current = 0;
  for (; ; ) {
    const res = await searchGlobal.searchGlobal(client, {
      query,
      filter,
      limit: Math.min(chunkSize, limit - current),
      minDate,
      maxDate,
      offset,
      onlyChannels
    });
    if (!res.length) return;
    for (const msg of res) {
      yield msg;
      if (++current >= limit) return;
    }
    if (!res.next) return;
    offset = res.next;
  }
}
exports.iterSearchGlobal = iterSearchGlobal;
