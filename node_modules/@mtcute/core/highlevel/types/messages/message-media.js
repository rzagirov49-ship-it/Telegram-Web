import { MtTypeAssertionError } from "../../../types/errors.js";
import { Contact as Contact$1 } from "../media/contact.js";
import { Dice as Dice$1 } from "../media/dice.js";
import { parseDocument } from "../media/document-utils.js";
import { Game as Game$2 } from "../media/game.js";
import { Invoice as Invoice$2 } from "../media/invoice.js";
import { LiveLocation as LiveLocation$1, Location as Location$1 } from "../media/location.js";
import { PaidMedia as PaidMedia$2 } from "../media/paid-media.js";
import { Photo as Photo$2 } from "../media/photo.js";
import { Poll as Poll$2 } from "../media/poll.js";
import { MediaStory as MediaStory$1 } from "../media/story.js";
import { TodoList as TodoList$2 } from "../media/todo.js";
import { Venue as Venue$2 } from "../media/venue.js";
import { WebPageMedia as WebPageMedia$2 } from "../media/web-page.js";
function _messageMediaFromTl(peers, m) {
  switch (m._) {
    case "messageMediaPhoto":
      if (!(m.photo?._ === "photo")) return null;
      return new Photo$2(m.photo, m);
    case "messageMediaDice":
      return new Dice$1(m);
    case "messageMediaContact":
      return new Contact$1(m);
    case "messageMediaDocument":
      if (!(m.document?._ === "document")) return null;
      return parseDocument(m.document, m);
    case "messageMediaGeo":
      if (!(m.geo._ === "geoPoint")) return null;
      return new Location$1(m.geo);
    case "messageMediaGeoLive":
      if (!(m.geo._ === "geoPoint")) return null;
      return new LiveLocation$1(m);
    case "messageMediaGame":
      return new Game$2(m.game);
    case "messageMediaWebPage":
      if (!(m.webpage._ === "webPage")) return null;
      return new WebPageMedia$2(m);
    case "messageMediaVenue":
      return new Venue$2(m);
    case "messageMediaPoll":
      if (!peers) {
        throw new MtTypeAssertionError("can't create poll without peers index", "PeersIndex", "null");
      }
      return new Poll$2(
        m.poll,
        peers,
        m.results,
        m.attachedMedia ? _messageMediaFromTl(peers, m.attachedMedia) : void 0
      );
    case "messageMediaInvoice": {
      const extended = m.extendedMedia?._ === "messageExtendedMedia" ? _messageMediaFromTl(peers, m.extendedMedia.media) : null;
      return new Invoice$2(m, extended);
    }
    case "messageMediaStory": {
      if (!peers) {
        throw new MtTypeAssertionError("can't create story without peers index", "PeersIndex", "null");
      }
      return new MediaStory$1(m, peers);
    }
    case "messageMediaPaidMedia": {
      const extended = [];
      m.extendedMedia.forEach((e) => {
        if (e._ !== "messageExtendedMedia") return;
        extended.push(_messageMediaFromTl(peers, e.media));
      });
      return new PaidMedia$2(m, extended);
    }
    case "messageMediaToDo":
      if (!peers) {
        throw new MtTypeAssertionError("can't create todo without peers index", "PeersIndex", "null");
      }
      return new TodoList$2(m, peers);
    default:
      return null;
  }
}
export {
  _messageMediaFromTl
};
