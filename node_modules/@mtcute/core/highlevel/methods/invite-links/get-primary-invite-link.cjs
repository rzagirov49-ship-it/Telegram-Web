if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const peersIndex = require("../../types/peers/peers-index.cjs");
const chatInviteLink = require("../../types/peers/chat-invite-link.cjs");
async function getPrimaryInviteLink(client, chatId) {
  const res = await client.call({
    _: "messages.getExportedChatInvites",
    peer: await resolvePeer.resolvePeer(client, chatId),
    adminId: { _: "inputUserSelf" },
    limit: 1,
    revoked: false
  });
  if (res.invites[0]?._ !== "chatInviteExported") {
    throw new errors.MtTypeAssertionError(
      "messages.getExportedChatInvites (@ .invites[0])",
      "chatInviteExported",
      res.invites[0]?._
    );
  }
  if (!res.invites[0].permanent) {
    throw new errors.MtTypeAssertionError("messages.getExportedChatInvites (@ .invites[0].permanent)", "true", "false");
  }
  const peers = peersIndex.PeersIndex.from(res);
  return new chatInviteLink.ChatInviteLink(res.invites[0], peers);
}
exports.getPrimaryInviteLink = getPrimaryInviteLink;
