if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const typeAssertions = require("../../../utils/type-assertions.cjs");
const utils = require("../auth/utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const peerUtils = require("../../utils/peer-utils.cjs");
const errors$1 = require("../../types/errors.cjs");
async function setChatColor(client, params) {
  const { color, backgroundEmojiId, forProfile } = params;
  const peer = await resolvePeer.resolvePeer(client, params.peer ?? "me");
  if (peerUtils.isInputPeerChannel(peer)) {
    const res = await client.call({
      _: "channels.updateColor",
      channel: peerUtils.toInputChannel(peer),
      color,
      backgroundEmojiId
    });
    client.handleClientUpdate(res);
    return;
  }
  if (peerUtils.isInputPeerUser(peer)) {
    if (!utils.isSelfPeer(client, peer)) {
      throw new errors.MtTypeAssertionError("setChatColor", "self", peer._);
    }
    const r = await client.call({
      _: "account.updateColor",
      color: {
        _: "peerColor",
        color,
        backgroundEmojiId
      },
      forProfile
    });
    typeAssertions.assertTrue("account.updateColor", r);
  }
  throw new errors$1.MtInvalidPeerTypeError(peer, "channel | user");
}
exports.setChatColor = setChatColor;
