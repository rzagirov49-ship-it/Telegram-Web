import { memoizeGetters } from "../../utils/memoize.js";
import { makeInspectable } from "../../utils/inspectable.js";
import { User as User$2 } from "../peers/user.js";
class BotStoppedUpdate {
  constructor(raw, _peers) {
    this.raw = raw;
    this._peers = _peers;
  }
  /**
   * ID of the user who stopped or restarted the bot
   */
  get userId() {
    return this.raw.userId;
  }
  /**
   * User who stopped or restarted the bot
   */
  get user() {
    return new User$2(this._peers.user(this.raw.userId));
  }
  /**
   * Whether the bot is currently stopped.
   *
   * If `true`, then the user has stopped the bot.
   * If `false`, then the user has re-started the bot.
   */
  get stopped() {
    return this.raw.stopped;
  }
}
const BotStoppedUpdate$1 = /* @__PURE__ */ memoizeGetters(BotStoppedUpdate, ["user"]);
const BotStoppedUpdate$2 = /* @__PURE__ */ makeInspectable(BotStoppedUpdate$1);
export {
  BotStoppedUpdate$2 as BotStoppedUpdate
};
