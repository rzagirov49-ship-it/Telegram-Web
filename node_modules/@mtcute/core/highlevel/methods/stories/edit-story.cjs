if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const normalizeInputMedia = require("../files/normalize-input-media.cjs");
const normalizePrivacyRules = require("../misc/normalize-privacy-rules.cjs");
const normalizeText = require("../misc/normalize-text.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const findInUpdate = require("./find-in-update.cjs");
async function editStory(client, params) {
  const { id, peer = "me", interactiveElements } = params;
  let caption;
  let entities;
  let media;
  if (params.media) {
    media = await normalizeInputMedia._normalizeInputMedia(client, params.media);
    if ("caption" in params.media && params.media.caption !== void 0) {
      [caption, entities] = await normalizeText._normalizeInputText(client, params.media.caption);
    }
  }
  if (params.caption) {
    [caption, entities] = await normalizeText._normalizeInputText(client, params.caption);
  }
  const privacyRules = params.privacyRules ? await normalizePrivacyRules._normalizePrivacyRules(client, params.privacyRules) : void 0;
  const res = await client.call({
    _: "stories.editStory",
    peer: await resolvePeer.resolvePeer(client, peer),
    id,
    media,
    mediaAreas: interactiveElements,
    caption,
    entities,
    privacyRules
  });
  return findInUpdate._findStoryInUpdate(client, res);
}
exports.editStory = editStory;
