import Long from "long";
import { assertTypeIs } from "../../../utils/type-assertions.js";
import { StickerSet as StickerSet$2 } from "../../types/misc/sticker-set.js";
async function getInstalledStickers(client) {
  const res = await client.call({
    _: "messages.getAllStickers",
    hash: Long.ZERO
  });
  assertTypeIs("getInstalledStickers", res, "messages.allStickers");
  return res.sets.map((set) => new StickerSet$2(set));
}
export {
  getInstalledStickers
};
