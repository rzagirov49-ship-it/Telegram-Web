import { resolveUser } from "../users/resolve-peer.js";
import { FullUser as FullUser$2 } from "../../types/peers/full-user.js";
async function getFullUser(client, userId) {
  const peer = await resolveUser(client, userId);
  const res = await client.call({
    _: "users.getFullUser",
    id: peer
  });
  return new FullUser$2(res);
}
export {
  getFullUser
};
