if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("../../updates/utils.cjs");
const peerUtils = require("../../utils/peer-utils.cjs");
const resolvePeerMany = require("../users/resolve-peer-many.cjs");
const errors = require("../../types/errors.cjs");
const user = require("../../types/peers/user.cjs");
async function deleteContacts(client, userIds) {
  if (!Array.isArray(userIds)) userIds = [userIds];
  const inputPeers = await resolvePeerMany.resolvePeerMany(client, userIds, peerUtils.toInputUser);
  if (!inputPeers.length) {
    throw new errors.MtInvalidPeerTypeError("all provided ids", "user");
  }
  const res = await client.call({
    _: "contacts.deleteContacts",
    id: inputPeers
  });
  utils.assertIsUpdatesGroup("contacts.deleteContacts", res);
  client.handleClientUpdate(res);
  return res.users.map((user$1) => new user.User(user$1));
}
exports.deleteContacts = deleteContacts;
