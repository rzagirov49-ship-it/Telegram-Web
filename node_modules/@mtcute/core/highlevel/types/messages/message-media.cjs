if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const contact = require("../media/contact.cjs");
const dice = require("../media/dice.cjs");
const documentUtils = require("../media/document-utils.cjs");
const game = require("../media/game.cjs");
const invoice = require("../media/invoice.cjs");
const location = require("../media/location.cjs");
const paidMedia = require("../media/paid-media.cjs");
const photo = require("../media/photo.cjs");
const poll = require("../media/poll.cjs");
const story = require("../media/story.cjs");
const todo = require("../media/todo.cjs");
const venue = require("../media/venue.cjs");
const webPage = require("../media/web-page.cjs");
function _messageMediaFromTl(peers, m) {
  switch (m._) {
    case "messageMediaPhoto":
      if (!(m.photo?._ === "photo")) return null;
      return new photo.Photo(m.photo, m);
    case "messageMediaDice":
      return new dice.Dice(m);
    case "messageMediaContact":
      return new contact.Contact(m);
    case "messageMediaDocument":
      if (!(m.document?._ === "document")) return null;
      return documentUtils.parseDocument(m.document, m);
    case "messageMediaGeo":
      if (!(m.geo._ === "geoPoint")) return null;
      return new location.Location(m.geo);
    case "messageMediaGeoLive":
      if (!(m.geo._ === "geoPoint")) return null;
      return new location.LiveLocation(m);
    case "messageMediaGame":
      return new game.Game(m.game);
    case "messageMediaWebPage":
      if (!(m.webpage._ === "webPage")) return null;
      return new webPage.WebPageMedia(m);
    case "messageMediaVenue":
      return new venue.Venue(m);
    case "messageMediaPoll":
      if (!peers) {
        throw new errors.MtTypeAssertionError("can't create poll without peers index", "PeersIndex", "null");
      }
      return new poll.Poll(
        m.poll,
        peers,
        m.results,
        m.attachedMedia ? _messageMediaFromTl(peers, m.attachedMedia) : void 0
      );
    case "messageMediaInvoice": {
      const extended = m.extendedMedia?._ === "messageExtendedMedia" ? _messageMediaFromTl(peers, m.extendedMedia.media) : null;
      return new invoice.Invoice(m, extended);
    }
    case "messageMediaStory": {
      if (!peers) {
        throw new errors.MtTypeAssertionError("can't create story without peers index", "PeersIndex", "null");
      }
      return new story.MediaStory(m, peers);
    }
    case "messageMediaPaidMedia": {
      const extended = [];
      m.extendedMedia.forEach((e) => {
        if (e._ !== "messageExtendedMedia") return;
        extended.push(_messageMediaFromTl(peers, e.media));
      });
      return new paidMedia.PaidMedia(m, extended);
    }
    case "messageMediaToDo":
      if (!peers) {
        throw new errors.MtTypeAssertionError("can't create todo without peers index", "PeersIndex", "null");
      }
      return new todo.TodoList(m, peers);
    default:
      return null;
  }
}
exports._messageMediaFromTl = _messageMediaFromTl;
