import { makeInspectable } from "../../utils/inspectable.js";
import { memoizeGetters } from "../../utils/memoize.js";
import { MessageEntity as MessageEntity$2 } from "../messages/message-entity.js";
class BusinessChatLink {
  constructor(raw) {
    this.raw = raw;
  }
  /** The link itself */
  get link() {
    return this.raw.link;
  }
  /** Text to be inserted into the message input */
  get text() {
    return this.raw.message;
  }
  /** Entities for the text */
  get entities() {
    return this.raw.entities?.map((x) => new MessageEntity$2(x)) ?? [];
  }
  /** Custom title for the link */
  get title() {
    return this.raw.title ?? null;
  }
  /** Number of clicks on the link */
  get clicks() {
    return this.raw.views;
  }
}
const BusinessChatLink$1 = /* @__PURE__ */ makeInspectable(BusinessChatLink);
const BusinessChatLink$2 = /* @__PURE__ */ memoizeGetters(BusinessChatLink$1, ["entities"]);
export {
  BusinessChatLink$2 as BusinessChatLink
};
