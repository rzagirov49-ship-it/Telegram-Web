if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const peerUtils = require("../../../utils/peer-utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const getDiscussionMessage = require("./get-discussion-message.cjs");
const getMessages = require("./get-messages.cjs");
const chat = require("../../../utils/links/chat.cjs");
async function getMessageByLink(client, link) {
  const parsed = chat.message.parse(link);
  if (!parsed) {
    throw new errors.MtArgumentError(`Invalid message link: ${link}`);
  }
  let peer;
  if ("username" in parsed) {
    peer = await resolvePeer.resolvePeer(client, parsed.username);
  } else {
    peer = await resolvePeer.resolvePeer(client, peerUtils.toggleChannelIdMark(parsed.channelId));
  }
  let msgId = parsed.id;
  if (parsed.commentId) {
    [peer] = await getDiscussionMessage._getDiscussionMessage(client, peer, parsed.id);
    msgId = parsed.commentId;
  }
  const [msg] = await getMessages.getMessages(client, peer, msgId);
  return msg;
}
exports.getMessageByLink = getMessageByLink;
