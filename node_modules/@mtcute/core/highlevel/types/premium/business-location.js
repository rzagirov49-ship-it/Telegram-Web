import { makeInspectable } from "../../utils/inspectable.js";
import { memoizeGetters } from "../../utils/memoize.js";
import { Location as Location$1 } from "../media/location.js";
class BusinessLocation {
  constructor(raw) {
    this.raw = raw;
  }
  /** Address of the business */
  get address() {
    return this.raw.address;
  }
  get location() {
    if (!this.raw.geoPoint || this.raw.geoPoint._ === "geoPointEmpty") return null;
    return new Location$1(this.raw.geoPoint);
  }
}
const BusinessLocation$1 = /* @__PURE__ */ makeInspectable(BusinessLocation);
const BusinessLocation$2 = /* @__PURE__ */ memoizeGetters(BusinessLocation$1, ["location"]);
export {
  BusinessLocation$2 as BusinessLocation
};
