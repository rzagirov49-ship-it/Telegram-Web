if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const typeAssertions = require("../../../utils/type-assertions.cjs");
const peersIndex = require("../../types/peers/peers-index.cjs");
const businessConnection = require("../../types/premium/business-connection.cjs");
const utils = require("../../updates/utils.cjs");
async function getBusinessConnection(client, connectionId) {
  const res = await client.call({
    _: "account.getBotBusinessConnection",
    connectionId
  });
  utils.assertIsUpdatesGroup("account.getBotBusinessConnection", res);
  client.handleClientUpdate(res);
  typeAssertions.assertTypeIs("account.getBotBusinessConnection", res.updates[0], "updateBotBusinessConnect");
  const peers = peersIndex.PeersIndex.from(res);
  return new businessConnection.BusinessConnection(res.updates[0].connection, peers);
}
exports.getBusinessConnection = getBusinessConnection;
