if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const typeAssertions = require("../../../utils/type-assertions.cjs");
const normalizeFileToDocument = require("../files/normalize-file-to-document.cjs");
const normalizeInputMedia = require("../files/normalize-input-media.cjs");
function isInputMediaSticker(media) {
  return typeof media === "object" && media !== null && "type" in media && media.type === "sticker";
}
async function setBusinessIntro(client, intro) {
  let tlIntro;
  if (intro) {
    let sticker;
    if (intro.sticker) {
      if (isInputMediaSticker(intro.sticker)) {
        const media = await normalizeInputMedia._normalizeInputMedia(client, intro.sticker, void 0, true);
        typeAssertions.assertTypeIs("_normalizeInputMedia", media, "inputMediaDocument");
        sticker = media.id;
      } else {
        sticker = await normalizeFileToDocument._normalizeFileToDocument(client, intro.sticker, {});
      }
    }
    tlIntro = {
      _: "inputBusinessIntro",
      title: intro.title ?? "",
      description: intro.description ?? "",
      sticker
    };
  }
  const res = await client.call({
    _: "account.updateBusinessIntro",
    intro: tlIntro
  });
  typeAssertions.assertTrue("account.updateBusinessIntro", res);
}
exports.setBusinessIntro = setBusinessIntro;
