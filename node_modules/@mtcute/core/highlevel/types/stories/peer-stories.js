import { isNotNull } from "@fuman/utils";
import { memoizeGetters } from "../../utils/memoize.js";
import { parsePeer } from "../peers/peer.js";
import { Story as Story$2 } from "./story.js";
import { makeInspectable } from "../../utils/inspectable.js";
class PeerStories {
  constructor(raw, _peers) {
    this.raw = raw;
    this._peers = _peers;
  }
  /**
   * Peer that owns these stories.
   */
  get peer() {
    return parsePeer(this.raw.peer, this._peers);
  }
  /**
   * ID of the last read story of this peer.
   */
  get maxReadId() {
    return this.raw.maxReadId ?? 0;
  }
  /**
   * List of peer stories.
   */
  get stories() {
    return this.raw.stories.map((it) => {
      if (it._ !== "storyItem") return null;
      return new Story$2(it, this._peers);
    }).filter(isNotNull);
  }
}
const PeerStories$1 = /* @__PURE__ */ memoizeGetters(PeerStories, ["peer", "stories"]);
const PeerStories$2 = /* @__PURE__ */ makeInspectable(PeerStories$1);
export {
  PeerStories$2 as PeerStories
};
