import { tl } from '../../../tl/index.js';
import { ReactionEmoji } from './types.js';
/**
 * Reaction count
 */
export declare class ReactionCount {
    readonly raw: tl.RawReactionCount;
    constructor(raw: tl.RawReactionCount);
    /**
     * Emoji representing the reaction
     */
    get emoji(): ReactionEmoji;
    /** Whether this is a paid reaction */
    get isPaid(): boolean;
    /**
     * Number of users who reacted with this emoji
     */
    get count(): number;
    /**
     * If the current user has reacted with this emoji,
     * this field will contain the order in which the
     * reaction was added.
     */
    get order(): number | null;
}
