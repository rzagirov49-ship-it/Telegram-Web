import { makeInspectable } from "../../utils/inspectable.js";
import { memoizeGetters } from "../../utils/memoize.js";
import { parseDocument } from "../media/document-utils.js";
class BusinessIntro {
  constructor(raw) {
    this.raw = raw;
  }
  /**
   * Title of the intro.
   */
  get title() {
    return this.raw.title;
  }
  /**
   * Description of the intro.
   */
  get description() {
    return this.raw.description;
  }
  /**
   * Sticker of the intro.
   */
  get sticker() {
    if (!this.raw.sticker || this.raw.sticker._ === "documentEmpty") return null;
    const doc = parseDocument(this.raw.sticker);
    if (doc.type !== "sticker") return null;
    return doc;
  }
}
const BusinessIntro$1 = /* @__PURE__ */ makeInspectable(BusinessIntro);
const BusinessIntro$2 = /* @__PURE__ */ memoizeGetters(BusinessIntro$1, ["sticker"]);
export {
  BusinessIntro$2 as BusinessIntro
};
