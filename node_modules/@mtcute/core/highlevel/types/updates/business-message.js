import { Message as Message$2 } from "../messages/message.js";
class BusinessMessage extends Message$2 {
  constructor(update, _peers) {
    super(update.message, _peers);
    this.update = update;
  }
  /**
   * Unique identifier of the business connection from which the message was received.
   */
  get connectionId() {
    return this.update.connectionId;
  }
  get groupedIdUnique() {
    const superGroupedIdUnique = super.groupedIdUnique;
    if (!superGroupedIdUnique) {
      return null;
    }
    return `${super.groupedIdUnique}|${this.update.connectionId}`;
  }
  /** The replied message (if any) */
  get replyTo() {
    return this.update.replyToMessage ? new Message$2(this.update.replyToMessage, this._peers) : null;
  }
}
export {
  BusinessMessage
};
