import { assertTypeIsNot } from "../../../utils/type-assertions.js";
import { AllStories as AllStories$2 } from "../../types/stories/all-stories.js";
async function getAllStories(client, params) {
  if (!params) params = {};
  const { offset, archived } = params;
  const res = await client.call({
    _: "stories.getAllStories",
    state: offset,
    next: Boolean(offset),
    hidden: archived
  });
  assertTypeIsNot("getAllStories", res, "stories.allStoriesNotModified");
  return new AllStories$2(res);
}
export {
  getAllStories
};
