if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const errors = require("../../../types/errors.cjs");
const logOut = require("./log-out.cjs");
const start = require("./start.cjs");
async function startTest(client, params) {
  if (!params) params = {};
  if (params.logout) {
    try {
      await logOut.logOut(client);
    } catch {
    }
  }
  const availableDcs = await client.call({
    _: "help.getConfig"
  }).then((res) => res.dcOptions);
  let phone = params.phone;
  if (phone) {
    if (!phone.match(/^99966\d{5}/)) {
      throw new errors.MtArgumentError(`${phone} is an invalid test phone number`);
    }
    const id = Number.parseInt(phone[5]);
    if (!availableDcs.find((dc) => dc.id === id)) {
      throw new errors.MtArgumentError(`${phone} has invalid DC ID (${id})`);
    }
  } else {
    let dcId = await client.getPrimaryDcId();
    if (params.dcId) {
      if (!availableDcs.find((dc) => dc.id === params.dcId)) {
        throw new errors.MtArgumentError(`DC ID is invalid (${dcId})`);
      }
      dcId = params.dcId;
    }
    let numbers = Math.floor(Math.random() * 9999).toString();
    while (numbers.length !== 4) numbers += "0";
    phone = `99966${dcId}${numbers}`;
  }
  let code = "";
  return start.start(client, {
    phone,
    code: () => code,
    codeSentCallback: (sent) => {
      for (let i = 0; i < sent.length; i++) {
        code += phone[5];
      }
    }
  });
}
exports.startTest = startTest;
