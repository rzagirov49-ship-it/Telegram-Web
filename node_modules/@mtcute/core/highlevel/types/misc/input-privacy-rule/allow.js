const all = { _: "inputPrivacyValueAllowAll" };
const contacts = { _: "inputPrivacyValueAllowContacts" };
const closeFriends = {
  _: "inputPrivacyValueAllowCloseFriends"
};
function users(users2) {
  return {
    allow: true,
    users: Array.isArray(users2) ? users2 : [users2]
  };
}
function chatParticipants(chats) {
  return {
    allow: true,
    chats: Array.isArray(chats) ? chats : [chats]
  };
}
export {
  all,
  chatParticipants,
  closeFriends,
  contacts,
  users
};
