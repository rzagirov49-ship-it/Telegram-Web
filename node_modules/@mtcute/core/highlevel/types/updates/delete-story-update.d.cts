import { tl } from '../../../tl/index.js';
import { Peer, PeersIndex } from '../../types/peers/index.js';
/**
 * A story was deleted
 */
export declare class DeleteStoryUpdate {
    readonly raw: tl.RawUpdateStory;
    readonly _peers: PeersIndex;
    constructor(raw: tl.RawUpdateStory, _peers: PeersIndex);
    /**
     * Peer that owns these stories.
     */
    get peer(): Peer;
    /**
     * ID of the deleted story
     */
    get storyId(): number;
}
