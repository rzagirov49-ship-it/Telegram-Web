import { IKeyValueRepository } from '../../repository/key-value.js';
import { MemoryStorageDriver } from '../driver.js';
export declare class MemoryKeyValueRepository implements IKeyValueRepository {
    readonly _driver: MemoryStorageDriver;
    readonly state: Map<string, Uint8Array>;
    constructor(_driver: MemoryStorageDriver);
    set(key: string, value: Uint8Array): void;
    get(key: string): Uint8Array | null;
    delete(key: string): void;
    deleteAll(): void;
}
