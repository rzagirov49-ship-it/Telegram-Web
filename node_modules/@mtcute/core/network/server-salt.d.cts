import { mtp } from '../tl/index.js';
import { default as Long } from 'long';
export declare class ServerSaltManager {
    private _futureSalts;
    currentSalt: Long;
    isFetching: boolean;
    shouldFetchSalts(): boolean;
    setFutureSalts(salts: mtp.RawMt_future_salt[]): void;
    private _timer?;
    private _scheduleReplace;
    private _replaceAndScheduleNext;
    destroy(): void;
}
