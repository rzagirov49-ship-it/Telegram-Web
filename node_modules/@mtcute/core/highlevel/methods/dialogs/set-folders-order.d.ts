import { ITelegramClient } from '../../client.types.js';
/**
 * Reorder folders
 *
 * @param order  New order of folders (folder IDs, where default = 0)
 */
export declare function setFoldersOrder(client: ITelegramClient, order: number[]): Promise<void>;
