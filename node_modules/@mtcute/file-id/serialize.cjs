if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/file-id] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/file-id] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const utils = require("@fuman/utils");
const tlRuntime = require("@mtcute/tl-runtime");
const utils$1 = require("./utils.cjs");
const typesInner = require("./types-inner.cjs");
const SUFFIX = new Uint8Array([typesInner.CURRENT_VERSION, typesInner.PERSISTENT_ID_VERSION]);
function toFileId(location) {
  const loc = location.location;
  let type = location.type;
  if (loc._ === "web") type |= typesInner.WEB_LOCATION_FLAG;
  if (location.fileReference) type |= typesInner.FILE_REFERENCE_FLAG;
  const writer = tlRuntime.TlBinaryWriter.manual(loc._ === "web" ? utils.utf8.encodedLength(loc.url) + 32 : 100);
  writer.int(type);
  writer.int(location.dcId);
  if (location.fileReference) {
    writer.bytes(location.fileReference);
  }
  switch (loc._) {
    case "web":
      writer.string(loc.url);
      writer.long(loc.accessHash);
      break;
    case "photo":
      writer.long(loc.id);
      writer.long(loc.accessHash);
      switch (loc.source._) {
        case "legacy":
          writer.int(0);
          writer.long(loc.source.secret);
          break;
        case "thumbnail":
          writer.int(1);
          writer.int(loc.source.fileType);
          writer.int(loc.source.thumbnailType.charCodeAt(0));
          break;
        case "dialogPhoto":
          writer.int(loc.source.big ? 3 : 2);
          writer.int53(loc.source.id);
          writer.long(loc.source.accessHash);
          break;
        case "stickerSetThumbnail":
          writer.int(4);
          writer.long(loc.source.id);
          writer.long(loc.source.accessHash);
          break;
        case "fullLegacy":
          writer.int(5);
          writer.long(loc.source.volumeId);
          writer.long(loc.source.secret);
          writer.int(loc.source.localId);
          break;
        case "dialogPhotoLegacy":
          writer.int(loc.source.big ? 7 : 6);
          writer.int53(loc.source.id);
          writer.long(loc.source.accessHash);
          writer.long(loc.source.volumeId);
          writer.int(loc.source.localId);
          break;
        case "stickerSetThumbnailLegacy":
          writer.int(8);
          writer.long(loc.source.id);
          writer.long(loc.source.accessHash);
          writer.long(loc.source.volumeId);
          writer.int(loc.source.localId);
          break;
        case "stickerSetThumbnailVersion":
          writer.int(9);
          writer.long(loc.source.id);
          writer.long(loc.source.accessHash);
          writer.int(loc.source.version);
          break;
        default:
          utils$1.assertNever(loc.source);
      }
      break;
    case "common":
      writer.long(loc.id);
      writer.long(loc.accessHash);
      break;
    default:
      utils$1.assertNever();
  }
  const result = utils$1.telegramRleEncode(writer.result());
  const withSuffix = utils.u8.alloc(result.length + SUFFIX.length);
  withSuffix.set(result);
  withSuffix.set(SUFFIX, result.length);
  return utils.base64.encode(withSuffix, true);
}
exports.toFileId = toFileId;
