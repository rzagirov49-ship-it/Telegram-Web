class WorkerTimersManager {
  upsert;
  cancel;
  exists;
  constructor(invoker) {
    const bind = invoker.makeBinder("timers");
    this.upsert = bind("upsert");
    this.cancel = bind("cancel");
    this.exists = bind("exists");
  }
}
export {
  WorkerTimersManager
};
