function isByobCapableStream(stream) {
  try {
    const reader = stream.getReader({ mode: "byob" });
    reader.releaseLock();
    return true;
  } catch (e) {
    if (e instanceof TypeError) {
      return false;
    }
    throw e;
  }
}
export {
  isByobCapableStream
};
