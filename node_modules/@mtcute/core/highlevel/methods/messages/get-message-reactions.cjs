if (typeof globalThis !== "undefined" && !globalThis._MTCUTE_CJS_DEPRECATION_WARNED) {
  globalThis._MTCUTE_CJS_DEPRECATION_WARNED = true;
  console.warn("[@mtcute/core] CommonJS bundles are deprecated. They will be removed completely in one of the upcoming releases. No support is provided for CommonJS users. Please consider switching to ESM, it's " + (/* @__PURE__ */ new Date()).getFullYear() + " already.");
  console.warn("[@mtcute/core] Learn more about switching to ESM: https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c");
}
"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const peerUtils = require("../../../utils/peer-utils.cjs");
const typeAssertions = require("../../../utils/type-assertions.cjs");
const utils = require("../../updates/utils.cjs");
const resolvePeer = require("../users/resolve-peer.cjs");
const messageReactions = require("../../types/messages/message-reactions.cjs");
const peersIndex = require("../../types/peers/peers-index.cjs");
async function getMessageReactionsById(client, chatId, messages) {
  const res = await client.call({
    _: "messages.getMessagesReactions",
    peer: await resolvePeer.resolvePeer(client, chatId),
    id: messages
  });
  utils.assertIsUpdatesGroup("messages.getMessagesReactions", res);
  client.handleClientUpdate(res);
  const index = {};
  const peers = peersIndex.PeersIndex.from(res);
  for (const update of res.updates) {
    typeAssertions.assertTypeIs("messages.getMessagesReactions", update, "updateMessageReactions");
    index[update.msgId] = new messageReactions.MessageReactions(update.msgId, peerUtils.getMarkedPeerId(update.peer), update.reactions, peers);
  }
  return messages.map((messageId) => index[messageId] ?? null);
}
async function getMessageReactions(client, messages) {
  return getMessageReactionsById(
    client,
    messages[0].chat.inputPeer,
    messages.map((it) => it.id)
  );
}
exports.getMessageReactions = getMessageReactions;
exports.getMessageReactionsById = getMessageReactionsById;
